import { Action as BaseAction } from '@sipin/siyue-admin-components';

export class Action extends BaseAction {
  // async transformGetListData(data) {
  //   return transformList(data);
  // }
  // async transformCreateParams(...params) {
  //   params[0].agencyId = '1';
  //   return [{ ...params[0] }];
  // }
  // async transformGetItemParams(code, ...params) {
  //   console.log(code);
  //   console.log(params);
  //   return [{ code }];
  // }
  // async transformUpdateParams(...params) {
  //   return [{ ...params[0], ...params[1] }];
  // }
  // async transformRemoveParams({ rowKey }) {
  //   return [{ id: rowKey }];
  // }
}
